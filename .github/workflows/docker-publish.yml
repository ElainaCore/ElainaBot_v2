name: Build and Push Docker Image

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: read
  packages: write

jobs:
  build:
    uses: ElainaCore/ElainaBot_v2/.github/workflows/docker-publish.yml@ci-assets
    with:
      source_sha: ${{ github.sha }}
    secrets: inherit
