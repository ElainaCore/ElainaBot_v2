name: CI

on:
  push:
    branches:
      - main
  pull_request:

permissions:
  contents: read

jobs:
  check:
    uses: ElainaCore/ElainaBot_v2/.github/workflows/ci.yml@ci-assets
    with:
      source_sha: ${{ github.sha }}
